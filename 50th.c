#include<stdio.h>
void main()
{
    int i=0;
    while(i<10001)  /*here the < symbol is used because it is increment order*/
   
   
    // in while loop we only have to give contions in within the bracket not like the for loop 
    {
        printf("The value of i is:%d\n",i);

         ++i; 
    }

}